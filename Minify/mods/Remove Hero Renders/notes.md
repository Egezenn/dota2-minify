<!-- LANG:EN -->
Removes hero previews, renders from the UI.

The console variable `map_enable_portrait_worlds 0` achieves the same goal, however you'll get stuck on MVP screens.

<!-- LANG:BG -->
Премахва визуализациите на героите и рендерите от потребителския интерфейс.

Конзолната променлива `map_enable_portrait_worlds 0` постига същата цел, но ще се затрудните на MVP екраните.

<!-- LANG:CS -->
Odstraňuje náhledy hrdinů a rendery z uživatelského rozhraní.

Proměnná konzole `map_enable_portrait_worlds 0` dosahuje stejného cíle, ale uvíznete na obrazovkách MVP.

<!-- LANG:DA -->
Fjerner forhåndsvisninger og gengivelser af helte fra brugergrænsefladen.

Konsolvariablen `map_enable_portrait_worlds 0` opnår det samme mål, men du vil sidde fast på MVP-skærme.

<!-- LANG:DE -->
Entfernt die Heldenvorschau und die Renderings aus der Benutzeroberfläche.

Die Konsolenvariable `map_enable_portrait_worlds 0` bewirkt dasselbe, allerdings bleiben Sie dann auf den MVP-Bildschirmen hängen.

<!-- LANG:EL -->
Αφαιρεί τις προεπισκοπήσεις και τις αποδόσεις των ηρώων από το UI.

Η μεταβλητή κονσόλας `map_enable_portrait_worlds 0` επιτυγχάνει τον ίδιο στόχο, ωστόσο θα κολλήσετε σε οθόνες MVP.

<!-- LANG:ES -->
Elimina las vistas previas de los héroes y las representaciones de la interfaz de usuario.

La variable de consola `map_enable_portrait_worlds 0` logra el mismo objetivo, pero te quedarás atascado en las pantallas MVP.

<!-- LANG:FI -->
Poistaa käyttöliittymästä sankarien esikatselut ja renderöinnit.

Konsoli-muuttuja `map_enable_portrait_worlds 0` saavuttaa saman tavoitteen, mutta juutut MVP-näyttöihin.

<!-- LANG:FR -->
Supprime les aperçus des héros et leur rendu depuis l'interface utilisateur.

La variable de console `map_enable_portrait_worlds 0` permet d'obtenir le même résultat, mais vous risquez de rester bloqué sur les écrans MVP.

<!-- LANG:HU -->
Eltávolítja a hős előnézeteket és rendereléseket a felhasználói felületről.

A `map_enable_portrait_worlds 0` konzolváltozó ugyanazt a célt éri el, azonban az MVP képernyőkön elakadhatsz.

<!-- LANG:IT -->
Rimuove le anteprime e i rendering degli eroi dall'interfaccia utente.

La variabile della console `map_enable_portrait_worlds 0` raggiunge lo stesso obiettivo, tuttavia rimarrai bloccato sulle schermate MVP.

<!-- LANG:NL -->
Verwijdert hero-previews en renders uit de gebruikersinterface.

De consolevariabele `map_enable_portrait_worlds 0` bereikt hetzelfde doel, maar je blijft dan hangen op MVP-schermen.

<!-- LANG:NO -->
Fjerner forhåndsvisninger og gjengivelser av helter fra brukergrensesnittet.

Konsollvariabelen `map_enable_portrait_worlds 0` oppnår samme mål, men du vil bli sittende fast på MVP-skjermer.

<!-- LANG:PL -->
Usuwa podglądy bohaterów i renderuje je z interfejsu użytkownika.

Zmienna konsoli `map_enable_portrait_worlds 0` osiąga ten sam cel, jednak utkniesz na ekranach MVP.

<!-- LANG:PT -->
Remove as pré-visualizações dos heróis e as renderizações da interface do usuário.

A variável de console `map_enable_portrait_worlds 0` alcança o mesmo objetivo, porém você ficará preso nas telas de MVP (Maior Propósito Viável).

<!-- LANG:RO -->
Elimină previzualizările și randările eroilor din interfața cu utilizatorul.

Variabila de consolă `map_enable_portrait_worlds 0` atinge același obiectiv, însă veți rămâne blocați pe ecranele MVP.

<!-- LANG:RU -->
Удаляет предварительный просмотр героев и рендеры из пользовательского интерфейса.

Консольная переменная `map_enable_portrait_worlds 0` достигает той же цели, однако вы застрянете на экранах MVP.

<!-- LANG:SV -->
Tar bort förhandsvisningar och renderingar av hjältar från användargränssnittet.

Konsolvariabeln `map_enable_portrait_worlds 0` uppnår samma mål, men du kommer att fastna på MVP-skärmar.

<!-- LANG:TR -->
Kahraman önizlemelerini ve kullanıcı arayüzünden görüntüleri kaldırır.

Konsol değişkeni `map_enable_portrait_worlds 0` da aynı amaca hizmet eder, ancak MVP ekranlarında takılıp kalırsınız.

<!-- LANG:UK -->
Видаляє попередній перегляд та рендеринг героїв з інтерфейсу користувача.

Консольна змінна `map_enable_portrait_worlds 0` досягає тієї ж мети, проте ви застрягнете на екранах MVP.