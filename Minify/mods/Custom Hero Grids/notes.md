<!-- LANG:EN -->
Imports custom hero grids (without breaking your own) from a remote source or a local configuration file into your Dota 2 hero grid selection.

Available for viewing at <https://egezenn.github.io/dota2-precompiled-grids>

You can drop your own `hero_grid_config.json` into the mod's `config` folder to use it instead of the remote version.

<!-- LANG:BG -->
Импортира персонализирани мрежи на герои (без да нарушава вашата собствена) от отдалечен източник или локален конфигурационен файл във вашата селекция от мрежа на герои в Dota 2.

Достъпно за преглед на <https://egezenn.github.io/dota2-precompiled-grids>

Можете да пуснете свой собствен `hero_grid_config.json` в папката `config` на мода, за да го използвате вместо отдалечената версия.

<!-- LANG:CS -->
Importuje vlastní mřížky hrdinů (bez narušení vaší vlastní) ze vzdáleného zdroje nebo lokálního konfiguračního souboru do vašeho výběru mřížky hrdinů v Dota 2.

K dispozici k prohlédnutí na <https://egezenn.github.io/dota2-precompiled-grids>

Můžete vložit svůj vlastní soubor `hero_grid_config.json` do složky `config` modu a použít ho místo vzdálené verze.

<!-- LANG:DA -->
Importerer brugerdefinerede heltegitter (uden at ødelægge dine egne) fra en fjernkilde eller en lokal konfigurationsfil til dit Dota 2-heltegittervalg.

Tilgængelig for visning på <https://egezenn.github.io/dota2-precompiled-grids>

Du kan placere din egen `hero_grid_config.json` i moddens `config`-mappe for at bruge den i stedet for fjernversionen.

<!-- LANG:DE -->
Importiert benutzerdefinierte Heldenraster (ohne Ihre eigenen zu beeinträchtigen) von einer externen Quelle oder einer lokalen Konfigurationsdatei in Ihre Dota 2-Heldenrasterauswahl.

Verfügbar unter <https://egezenn.github.io/dota2-precompiled-grids>

Sie können Ihre eigene `hero_grid_config.json` in den `config`-Ordner der Mod kopieren, um sie anstelle der externen Version zu verwenden.

<!-- LANG:EL -->
Εισάγει προσαρμοσμένα πλέγματα ηρώων (χωρίς να παραβιάζει τα δικά σας) από μια απομακρυσμένη πηγή ή ένα τοπικό αρχείο διαμόρφωσης στην επιλογή πλέγματος ηρώων Dota 2.

Διαθέσιμο για προβολή στη διεύθυνση <https://egezenn.github.io/dota2-precompiled-grids>

Μπορείτε να αποθέσετε το δικό σας `hero_grid_config.json` στον φάκελο `config` του mod για να το χρησιμοποιήσετε αντί για την απομακρυσμένη έκδοση.

<!-- LANG:ES -->
Importa cuadrículas de héroes personalizadas (sin modificar las tuyas) desde una fuente remota o un archivo de configuración local a tu selección de héroes de Dota 2.

Disponible para su visualización en <https://egezenn.github.io/dota2-precompiled-grids>

Puedes colocar tu propio archivo `hero_grid_config.json` en la carpeta `config` del mod para usarlo en lugar de la versión remota.

<!-- LANG:FI -->
Tuo mukautettuja sankariruudukoita (omaa rikkomatta) etälähteestä tai paikallisesta asetustiedostosta Dota 2 -sankariruudukkovalikoimaasi.

Saatavilla osoitteessa <https://egezenn.github.io/dota2-precompiled-grids>

Voit pudottaa oman `hero_grid_config.json`-tiedostosi modin `config`-kansioon käyttääksesi sitä etäversion sijaan.

<!-- LANG:FR -->
Importe des grilles de héros personnalisées (sans altérer les vôtres) depuis une source distante ou un fichier de configuration local dans votre sélection de grilles de héros pour Dota 2.

Disponible à l'adresse <https://egezenn.github.io/dota2-precompiled-grids>

Vous pouvez utiliser votre propre fichier `hero_grid_config.json` dans le dossier `config` du mod à la place de la version distante.

<!-- LANG:HU -->
Egyéni hősrácsokat importál (a sajátjaid feltörése nélkül) távoli forrásból vagy helyi konfigurációs fájlból a Dota 2 hősrács-kiválasztásodba.

Elérhető a <https://egezenn.github.io/dota2-precompiled-grids> címen.

A saját `hero_grid_config.json` fájlodat beillesztheted a mod `config` mappájába, hogy azt használd a távoli verzió helyett.

<!-- LANG:IT -->
Importa griglie degli eroi personalizzate (senza modificare le tue) da una fonte remota o da un file di configurazione locale nella selezione delle griglie degli eroi di Dota 2.

Disponibile per la visualizzazione all'indirizzo <https://egezenn.github.io/dota2-precompiled-grids>

Puoi inserire il tuo file `hero_grid_config.json` nella cartella `config` della mod per utilizzarlo al posto della versione remota.

<!-- LANG:NL -->
Importeert aangepaste heldenroosters (zonder je eigen rooster te beschadigen) vanuit een externe bron of een lokaal configuratiebestand naar je Dota 2-heldenrooster.

Beschikbaar om te bekijken op <https://egezenn.github.io/dota2-precompiled-grids>

Je kunt je eigen `hero_grid_config.json` in de `config`-map van de mod plaatsen om deze te gebruiken in plaats van de externe versie.

<!-- LANG:NO -->
Importerer tilpassede helte-rutenett (uten å ødelegge dine egne) fra en ekstern kilde eller en lokal konfigurasjonsfil til ditt Dota 2-helte-rutenettvalg.

Tilgjengelig for visning på <https://egezenn.github.io/dota2-precompiled-grids>

Du kan slippe din egen `hero_grid_config.json` i moddens `config`-mappe for å bruke den i stedet for den eksterne versjonen.

<!-- LANG:PL -->
Importuje niestandardowe siatki bohaterów (bez psucia własnych) ze zdalnego źródła lub lokalnego pliku konfiguracyjnego do wybranej siatki bohaterów w Dota 2.

Dostępne do wglądu pod adresem <https://egezenn.github.io/dota2-precompiled-grids>

Możesz umieścić swój własny plik `hero_grid_config.json` w folderze `config` moda, aby używać go zamiast wersji zdalnej.

<!-- LANG:PT -->
Importa grids de heróis personalizados (sem quebrar os seus) de uma fonte remota ou de um arquivo de configuração local para a sua seleção de grids de heróis do Dota 2.

Disponível para visualização em <https://egezenn.github.io/dota2-precompiled-grids>

Você pode colocar seu próprio arquivo `hero_grid_config.json` na pasta `config` do mod para usá-lo em vez da versão remota.

<!-- LANG:RO -->
Importă grile de eroi personalizate (fără a le modifica pe ale tale) dintr-o sursă la distanță sau dintr-un fișier de configurare local în selecția ta de grile de eroi Dota 2.

Disponibil pentru vizualizare la <https://egezenn.github.io/dota2-precompiled-grids>

Poți plasa propriul fișier `hero_grid_config.json` în folderul `config` al modului pentru a-l utiliza în locul versiunii la distanță.

<!-- LANG:RU -->
Импортирует пользовательские сетки героев (без нарушения ваших собственных) из удалённого источника или локального файла конфигурации в вашу сетку героев Dota 2.

Доступно для просмотра по адресу <https://egezenn.github.io/dota2-precompiled-grids>

Вы можете поместить свой собственный файл `hero_grid_config.json` в папку `config` мода, чтобы использовать его вместо удалённой версии.

<!-- LANG:SV -->
Importerar anpassade hjälte-rutnät (utan att förstöra dina egna) från en fjärrkälla eller en lokal konfigurationsfil till ditt Dota 2-hjälte-rutnätsval.

Tillgänglig för visning på <https://egezenn.github.io/dota2-precompiled-grids>

Du kan släppa din egen `hero_grid_config.json` i moddens `config`-mapp för att använda den istället för fjärrversionen.

<!-- LANG:TR -->
Özel kahraman ızgaralarını (kendi ızgaralarınızı bozmadan) uzak bir kaynaktan veya yerel bir yapılandırma dosyasından Dota 2 kahraman ızgarası seçiminize aktarır.

Şuradan görüntüleyebilirsiniz: <https://egezenn.github.io/dota2-precompiled-grids>

Uzak sürüm yerine kullanmak için kendi `hero_grid_config.json` dosyanızı modun `config` klasörüne bırakabilirsiniz.

<!-- LANG:UK -->
Імпортує власні сітки героїв (без порушення ваших власних) з віддаленого джерела або локального файлу конфігурації у ваш вибір сітки героїв Dota 2.

Доступно для перегляду за адресою <https://egezenn.github.io/dota2-precompiled-grids>

Ви можете перетягнути власний файл `hero_grid_config.json` у папку `config` мода, щоб використовувати його замість віддаленої версії.
