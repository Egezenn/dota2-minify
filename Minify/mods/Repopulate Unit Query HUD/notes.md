<!-- LANG:EN -->
Fixes and repopulates the Unit Query HUD panel that was released in patch 7.00.

Available with the console variable `dota_hud_new_query_panel 1`.

<!-- LANG:BG -->
Поправя и пренарежда панела за заявки към единици (Unit Query HUD), който беше пуснат в пач 7.00.

Достъпно с конзолната променлива `dota_hud_new_query_panel 1`.

<!-- LANG:CS -->
Opravuje a znovu naplňuje panel HUD pro dotazy jednotek, který byl vydán v aktualizaci 7.00.

K dispozici s proměnnou konzole `dota_hud_new_query_panel 1`.

<!-- LANG:DA -->
Retter og genudfylder Unit Query HUD-panelet, der blev udgivet i patch 7.00.

Tilgængelig med konsolvariablen `dota_hud_new_query_panel 1`.

<!-- LANG:DE -->
Behebt einen Fehler im mit Patch 7.00 eingeführten Einheiten-Abfrage-HUD und aktualisiert es.

Verfügbar über die Konsolenvariable `dota_hud_new_query_panel 1`.

<!-- LANG:EL -->
Διορθώνει και συμπληρώνει ξανά τον πίνακα HUD του Unit Query που κυκλοφόρησε στην ενημέρωση κώδικα 7.00.

Διατίθεται με τη μεταβλητή κονσόλας `dota_hud_new_query_panel 1`.

<!-- LANG:ES -->
﻿Corrige y repuebla el panel HUD de consulta de unidades que se lanzó en el parche 7.00.

Disponible con la variable de consola `dota_hud_new_query_panel 1`.

<!-- LANG:FI -->
Korjaa ja täyttää uudelleen Unit Query HUD -paneelin, joka julkaistiin päivityksessä 7.00.

Saatavilla konsolimuuttujalla `dota_hud_new_query_panel 1`.

<!-- LANG:FR -->
Corrige et rétablit le panneau d'affichage des requêtes d'unités introduit dans le patch 7.00.

Disponible avec la variable de console `dota_hud_new_query_panel 1`.

<!-- LANG:HU -->
Kijavítja és újratölti az Egységlekérdezés HUD panelt, amely a 7.00-s javításban jelent meg.

Elérhető a `dota_hud_new_query_panel 1` konzolváltozóval.

<!-- LANG:IT -->
Corregge e ripopola il pannello HUD delle query unità rilasciato nella patch 7.00.

Disponibile con la variabile console `dota_hud_new_query_panel 1`.

<!-- LANG:NL -->
Herstelt en vult het Unit Query HUD-paneel opnieuw, dat werd uitgebracht in patch 7.00.

Beschikbaar met de consolevariabele `dota_hud_new_query_panel 1`.

<!-- LANG:NO -->
Retter og fyller ut Unit Query HUD-panelet som ble utgitt i patch 7.00 på nytt.

Tilgjengelig med konsollvariabelen `dota_hud_new_query_panel 1`.

<!-- LANG:PL -->
Naprawia i ponownie wypełnia panel HUD zapytania jednostki, który został udostępniony w aktualizacji 7.00.

Dostępne ze zmienną konsoli `dota_hud_new_query_panel 1`.

<!-- LANG:PT -->
Corrige e repopular o painel HUD de consulta de unidades lançado no patch 7.00.

Disponível com a variável de console `dota_hud_new_query_panel 1`.

<!-- LANG:RO -->
Corectează și repopulează panoul HUD Unit Query lansat în patch-ul 7.00.

Disponibil cu variabila de consolă `dota_hud_new_query_panel 1`.

<!-- LANG:RU -->
﻿Исправляет и повторно заполняет панель HUD запроса юнита, выпущенную в патче 7.00.

Доступно с консольной переменной `dota_hud_new_query_panel 1`.

<!-- LANG:SV -->
Åtgärdar och fyller i Unit Query HUD-panelen som släpptes i patch 7.00.

Tillgänglig med konsolvariabeln `dota_hud_new_query_panel 1`.

<!-- LANG:TR -->
﻿7.00 yamasında yayınlanan Birim Sorgulama HUD panelini düzeltir ve yeniden doldurur.

Konsol değişkeni `dota_hud_new_query_panel 1` ile kullanılabilir.

<!-- LANG:UK -->
Виправляє та повторно заповнює панель HUD запитів юнітів, яка була випущена у патчі 7.00.

Доступно зі змінною консолі `dota_hud_new_query_panel 1`.